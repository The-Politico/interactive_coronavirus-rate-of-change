import { pib as meta } from 'package.json';

export default {
  title: meta.pageName,
  description: 'No single metric can perfectly describe where the novel coronavirus has hit hardest. So we’re proposing two.',
};
